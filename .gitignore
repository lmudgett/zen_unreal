# Build trees and packaged output (regenerated by CMake / build-dist.bat)
/build/
/build-sdl/
/dist/
/Unreal - v200/

# Runtime artifacts
/Save/
*.log
Crash.txt
*.probe

# Retail CD image — exceeds GitHub's 100 MB file limit; stays local-only
/Unreal - v200/CD/

# Local tooling state
.claude/settings.local.json
.vs/
*.user

# Local backup of the pre-GitHub git history (delete after push is verified)
/.git-backup-pre-github/
